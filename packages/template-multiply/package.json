{
  "name": "@animo/template-multiply",
  "description": "Sample Multiply package",
  "main": "build/index",
  "types": "build/index",
  "version": "0.2.0",
  "files": [
    "build"
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/animo/typescript-project-template",
    "directory": "packages/template-multiply"
  },
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./build",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "jest"
  }
}
