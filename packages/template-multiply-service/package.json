{
  "name": "@animo/template-multiply-service",
  "description": "Multiplication service",
  "private": true,
  "main": "build/app",
  "types": "build/app",
  "version": "0.1.3",
  "files": [
    "build"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "@animo/template-multiply": "^0.2.0",
    "express": "^4.18.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/animo/typescript-project-template",
    "directory": "packages/template-multiply-service"
  },
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rimraf -rf ./build",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn run build",
    "test": "echo 'No tests for package @animo/template-multiply-service'",
    "start": "node build/app"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}
